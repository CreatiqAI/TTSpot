/// Feature switches. Flip and rebuild.
///
/// The Instagram-style feed (Explore, Following, likes, saves, polls, guides,
/// Car of the Week, clubs) is the first tab. Set to false to hide it and its
/// entry points (the app then starts on the map).
const kSocialFeed = true;
